SELECT ssl, version, cipher, protocol
FROM pg_stat_ssl
JOIN pg_stat_activity
  ON pg_stat_ssl.pid = pg_stat_activity.pid
WHERE pg_stat_activity.datname = current_database();


SHOW ssl_version;
SHOW ssl_cipher;
SHOW ssl_protocol;
